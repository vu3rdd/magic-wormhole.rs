[package]
name = "core-c"
version = "0.0.1"
authors = ["ram <ram@rkrishnan.org>"]

[target.'cfg(target_os="android")'.dependencies]
jni = { version = "0.5", default-features = false }

[lib]
crate-type = ["dylib"]

[dependencies]
magic-wormhole-core = { path = "../core" }